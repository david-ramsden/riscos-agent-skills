# RISC OS Command Syntax

## Syntax format

Symbols used in syntax descriptions:
`<>` mark sections to be filled in, e.g. `<filename>` indicates that a filename should be supplied here.
`[]` mark optional sections.
`|` indicates a choice, e.g. `A|B` means `A or B`.
