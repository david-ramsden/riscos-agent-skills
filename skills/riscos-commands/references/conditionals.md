# RISC OS Conditionals

* `*IF <expression> THEN <command> [ELSE <command>]`: *IF conditionally executes another command depending on the value of an expression.

* `*IfThere <file> Then <command> Else <command>`: *IfThere looks for a file and executes a choice of commands.
